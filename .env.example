# Telegram Bot Configuration
BOT_TOKEN=your_telegram_bot_token_here

# PDF Processing Settings
PAGES_PER_SEND=3
INTERVAL_HOURS=6
SCHEDULE_TIME=09:00
PDF_PATH=book.pdf

# File Paths
OUTPUT_DIR=output
UPLOAD_DIR=uploads
DATABASE_PATH=database.json

# File Validation and Storage Settings
MAX_FILE_SIZE_MB=50
IMAGE_RETENTION_DAYS=7
IMAGE_QUALITY=85

# Instructions:
# 1. Copy this file to .env
# 2. Replace 'your_telegram_bot_token_here' with your actual bot token from @BotFather
# 3. Adjust other settings as needed
# 4. Place your PDF file in the project directory and update PDF_PATH if needed
#
# New Settings:
# - MAX_FILE_SIZE_MB: Maximum size for uploaded PDF files (in MB)
# - IMAGE_RETENTION_DAYS: Number of days to keep generated page images
# - IMAGE_QUALITY: JPEG quality for generated images (1-100, higher = better quality)
#
# For production deployment:
# - Never commit .env files to version control
# - Use secrets management services (GitHub Actions secrets, Docker secrets, etc.)
# - Set environment variables through your deployment platform
# - Consider using different tokens for development and production